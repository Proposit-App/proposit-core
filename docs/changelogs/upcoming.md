# upcoming changelog

- Docs: retire the dead `docs/inbox/` change-request pattern in favour of `tcw work`. Rewrote the AGENTS.md incoming-work paragraph to the `tcw work inbox list`→`show`→`accept` flow, dropped the dead `.prettierignore` entry, and removed a `docs/inbox/...` pointer from a `core.test.ts` comment.
