# v1.9.0

- Building an argument now reliably settles on a conclusion. When several claims could each be the point, the builder picks the most strongly supported one instead of giving up — so an import no longer fails just because the conclusion was ambiguous.
