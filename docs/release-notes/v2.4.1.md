# v2.4.1 release notes

Patch release with no public-facing changes. This release is an internal
file-size refactor (splitting several large `src/lib/` files into smaller
modules) — no public API, behavior, or wire-format change. See
`docs/changelogs/v2.4.1.md` for the developer-facing detail.
