# Release Notes

- Replaced the unmaintained `js-yaml` dependency with the actively maintained `yaml` package for better YAML 1.2 spec compliance.
- Dependency formatting (package.json and pnpm-lock.yaml) is now automatically fixed after installs via a postinstall hook.
