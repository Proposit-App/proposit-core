# Release Notes

## New Features

- **Custom fields on toggled negation expressions.** `toggleNegation` now accepts an optional second argument with extra fields (e.g., creator, timestamp) to merge into the newly created NOT and formula expressions. This removes the need to patch these fields after the fact.
