dist/
docs/api/
coverage/
.claude/
.untracked/
# Inbox request docs are freeform, consumer-authored artifacts (prose,
# tables, pasted output) — not maintained project source. Don't gate
# `pnpm run check` on their prettier-format compliance. Covers the live
# inbox and its `.archive/`.
docs/inbox/
src/lib/core/parser/formula-gen.js
# Recording-provider outputs from `INGESTION_TEST_RECORD=1` runs.
# Machine-generated (`JSON.stringify(file, null, 2)` — 2-space
# indent) and re-overwritten on every re-record; not worth gating
# CI on prettier-format compliance.
test/extensions/pipelines/fixtures/**/v2-recorded-llm.json
test/extensions/pipelines/fixtures/**/scribe-recorded-llm.json
